// caller setup
      return value   // space reserved before calling a function via PUSHZ)
      function args  // pushed in reverse order
      return address // via CALL
// function setup       (via FRAME)
      saved bp
      saved sp
bp -> storage
      .
      .
      .
sp ->